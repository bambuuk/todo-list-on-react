import { Component } from 'react';

import './title.scss';

class Title extends Component {
    render() {
        return(
            <h1 className="title title_margin">Task List</h1>
        );
    }
}

export default Title;